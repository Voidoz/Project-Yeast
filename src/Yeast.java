public final class Yeast
{
	// Configuration class

	// Desired values

	public Yeast()
	{
		// Load desired values variables from file

		// Initialise classes
	}

	public static void main(String[] args)
	{
		// Initialise application

		// While not reached desired values
		while (true)
		{
			// Interface: Read Sensors

			// Find offset to previous data

			// Is not configuration appropriately configured to value offsets within
			// tolerance constraints
			if (true)
			{
				// Re-adjust configuration
				
				// Interface: Apply new configuration
			}
		}

		// Close application
	}
}
